<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RH Notification - Multiple Opportunities</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: linear-gradient(135deg, #28a745 0%, #20c997 100%); color: white; padding: 20px; border-radius: 10px; text-align: center; }
        .content { background: #f9f9f9; padding: 20px; border-radius: 10px; margin: 20px 0; }
        .user-info { background: #e8f4fd; padding: 15px; border-radius: 8px; margin: 15px 0; }
        .opportunities-summary { background: #fff; padding: 15px; border-radius: 8px; margin: 15px 0; border-left: 4px solid #28a745; }
        .opportunity-item { background: #f8f9fa; padding: 10px; border-radius: 5px; margin: 8px 0; border-left: 3px solid #28a745; }
        .opportunity-header { font-weight: bold; color: #28a745; margin-bottom: 5px; }
        .opportunity-details { font-size: 0.9rem; color: #666; }
        .detail-row { display: flex; justify-content: space-between; margin: 3px 0; }
        .detail-label { font-weight: bold; color: #555; }
        .detail-value { color: #333; }
        .value-highlight { color: #28a745; font-weight: bold; }
        .footer { text-align: center; color: #666; font-size: 12px; margin-top: 20px; }
        .stats { background: #fff3cd; padding: 10px; border-radius: 5px; margin: 10px 0; border-left: 4px solid #ffc107; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📧 RH Notification - Multiple Opportunities</h1>
        </div>
        
        <div class="content">
            <h2>Hello Administrator,</h2>
            
            <p>A RH user has notified you about multiple opportunities that require your attention.</p>
            
            <div class="user-info">
                <h3>👤 RH User Information:</h3>
                <p><strong>Name:</strong> {{ rh_user.name ?? 'Not specified' }}</p>
                <p><strong>Email:</strong> {{ rh_user.email }}</p>
                <p><strong>Role:</strong> RH</p>
                <p><strong>Notification Date:</strong> {{ "now"|date("Y-m-d H:i:s") }}</p>
            </div>
            
            <div class="stats">
                <h3>📊 Notification Summary:</h3>
                <p><strong>Total Opportunities Selected:</strong> {{ opportunities|length }}</p>
                <p><strong>Newly Notified:</strong> {{ notified_count }}</p>
                {% if already_notified_count > 0 %}
                <p><strong>Already Notified:</strong> {{ already_notified_count }}</p>
                {% endif %}
            </div>
            
            <div class="opportunities-summary">
                <h3>📋 Opportunities Details:</h3>
                
                {% for opportunity in opportunities %}
                <div class="opportunity-item">
                    <div class="opportunity-header">{{ opportunity.name }}</div>
                    <div class="opportunity-details">
                        <div class="detail-row">
                            <span class="detail-label">🏢 Company:</span>
                            <span class="detail-value">{{ opportunity.company }}</span>
                        </div>
                        <div class="detail-row">
                            <span class="detail-label">👤 Contact:</span>
                            <span class="detail-value">{{ opportunity.contact }}</span>
                        </div>
                        <div class="detail-row">
                            <span class="detail-label">💰 Value:</span>
                            <span class="detail-value value-highlight">{{ opportunity.value }} DT</span>
                        </div>
                        <div class="detail-row">
                            <span class="detail-label">📅 Deadline:</span>
                            <span class="detail-value">{{ opportunity.closeDate ? opportunity.closeDate|date('Y-m-d') : 'Not specified' }}</span>
                        </div>
                        {% if opportunity.description %}
                        <div class="detail-row">
                            <span class="detail-label">📝 Description:</span>
                            <span class="detail-value">{{ opportunity.description }}</span>
                        </div>
                        {% endif %}
                    </div>
                </div>
                {% endfor %}
            </div>
            
            <p><strong>Action Required:</strong> Please log into the system to review and make decisions on these opportunities.</p>
            
            <p style="background: #fff3cd; padding: 10px; border-radius: 5px; border-left: 4px solid #ffc107;">
                <strong>⚠️ Priority:</strong> These opportunities have been marked for immediate administrator review.
            </p>
        </div>
        
        <div class="footer">
            <p>This is an automated notification from the HR Management System.</p>
            <p>Please do not reply to this email.</p>
        </div>
    </div>
</body>
</html> 